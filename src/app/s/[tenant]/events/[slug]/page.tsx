// The microsite reuses the canonical event detail (RSVP, chat, live, pass all
// work) rendered inside the tenant's branded shell. Events are unique by slug.
export { default, generateMetadata } from "@/app/(main)/events/[slug]/page"
