import { ComingSoon } from "@/components/coming-soon"

export const metadata = { title: "Messages — HYKRZ" }

export default function MessagesPage() {
  return (
    <ComingSoon
      title="Messages & group chat"
      description="Every event will get its own WhatsApp-style group chat, plus direct messages between riders. We're building it for real — no placeholders."
    />
  )
}
